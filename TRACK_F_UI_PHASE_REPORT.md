# تقرير: بناء الواجهة الفعلية + Flutter Web/PWA + GitHub Pages

**التاريخ:** 2026-09-25
**المدخل:** `student_app_foundation.zip` (كما رفعه المستخدم — Foundation + Memory
Engine + Track A/B/C، Static-Audit-Passed، Runtime-Unverified)
**المخرج:** نفس المشروع، بعد إضافة طبقة الواجهة الكاملة + دعم الويب + النشر،
دون حذف أو تبسيط أي شيء من الأساس الموجود.

---

## 1. ماذا تم بناؤه فعليًا (ملفات حقيقية، ليست Demo)

### أ) الواجهة (lib/ui/) — 12 ملفًا جديدًا
- **الهوية الأولى**: `dashboard_screen.dart` يعرض حرفيًا العناوين الثلاثة
  المطلوبة: «ثانوية رابح بطاط» / «مرافق التلميذ» / «المرافقة والتنظيم
  الدراسي طوال العام».
- **5 أقسام رئيسية** عبر شريط تنقل سفلي (`home_shell.dart`):
  الرئيسية، المهام، التقدم (الإتقان+الذاكرة)، الأولويات، الامتحانات.
- **الإعداد الأولي** (`onboarding_screen.dart` + `student_repository.dart`
  الجديد): ينشئ فعليًا صف Student + AcademicYear حقيقيين في القاعدة —
  ليس شاشة شكلية.
- **كل شاشة تقرأ من الجداول الحقيقية** عبر الـRepositories الموجودة
  (ExamRepository، وRepositories جديدة: ContentRepository، TaskRepository
  لإدارة المواد/المهام التي لم يكن لها أي واجهة إدخال من قبل).
- **لا بيانات وهمية**: أي قسم لا توجد له بيانات بعد (الإتقان، الذاكرة،
  الأولويات — لأن محركاتها غير موصولة بعد، انظر القسم 3) يعرض حالة
  «لا توجد بيانات بعد» صادقة، لا أرقامًا مختلقة.

### ب) الحالات الحدّية المطلوبة صراحةً
| الحالة | التنفيذ |
|---|---|
| زر الرجوع الفيزيائي (Android) | `ExitConfirmScope`: أول ضغطة تُرجع لتبويب «الرئيسية»، ضغطة ثانية خلال ثانيتين للخروج، مع رسالة توضيحية |
| الضغط المزدوج/السريع | `DebouncedButton`: أي زر يكتب في القاعدة يُعطَّل فور الضغط حتى تنتهي العملية |
| التحميل/الانتظار/الأخطاء | `AsyncSection` موحّد: Loading/Error/Empty/Data لكل شاشة، بدل تكرار الكود |
| أول تشغيل / لا بيانات بعد التثبيت | تدفق `onboarding` إجباري قبل أي شاشة أخرى، مع تحقق فعلي من وجود Student في القاعدة عند كل إقلاع |
| فقدان/تكرار البيانات | كل عملية كتابة (مهمة، امتحان، طالب) داخل `transaction()` واحدة؛ الأزرار محصّنة ضد الضغط المزدوج |
| RTL والعربية | `Directionality` مفروضة صراحة + `Locale('ar')` + `GlobalMaterialLocalizations` |
| لوحة المفاتيح | كل نموذج داخل `SingleChildScrollView`/`Padding` يتفاعل مع `viewInsets.bottom` |
| الشاشات الصغيرة/الكبيرة | `ConstrainedBox(maxWidth: 480)` على نماذج الإدخال، `ListView`/`SafeArea` في كل مكان |
| إعادة فتح التطبيق | حالة الطالب تُقرأ من SharedPreferences وتُتحقق فعليًا من القاعدة (لا تُصدَّق بشكل أعمى) عند كل إقلاع |

### ج) Flutter Web / PWA
- `web/index.html`، `web/manifest.json` (RTL، الاسم، الألوان)، أيقونات
  PWA حقيقية (192/512 + نسخ maskable) تم توليدها فعليًا بصيغة PNG.
- `web/robots.txt` + `web/sitemap.xml` + وسوم SEO (title/description/
  Open Graph) داخل `index.html` جاهزة، مع علامة **TODO** واضحة في مكانين
  فقط (رابط GitHub Pages الفعلي) — لا يمكن تعبئتها من هذه البيئة لأنها
  تعتمد على اسم حسابك/مستودعك.
- **تعديل جوهري وضروري في قاعدة البيانات**: الاتصال الأصلي كان يستخدم
  `dart:io`/`NativeDatabase` مباشرة، وهذا **لا يعمل على الويب إطلاقًا**.
  تم فصله إلى backend خاص بالويب (`drift/wasm.dart`) يُختار تلقائيًا حسب
  المنصة — انظر **DEVIATION-8** (الأهم في القسم التالي).

### د) GitHub Actions / GitHub Pages
- `.github/workflows/deploy.yml`: يبني Flutter Web عند كل push إلى
  `main`، وينشر `build/web` مباشرة على GitHub Pages عبر Actions
  الرسمية (`actions/deploy-pages`)، مع ضبط `--base-href` تلقائيًا
  حسب اسم المستودع.

---

## 2. الأهم: ما لم يُتحقق منه فعليًا (لا ادّعاء بعكس ذلك)

**لا يوجد Flutter/Dart SDK ولا اتصال إنترنت في بيئة التنفيذ هنا.** كل ما
سبق تمت كتابته بعناية ومراجعته يدويًا (فحص توازن الأقواس عبر 62 ملف Dart
+ إعادة تشغيل `tools/check_repository_boundaries.sh` = ناجح)، لكن **لم
يُبنَ ولم يُشغَّل فعليًا**. أعلى المخاطر ترتيبًا:

1. **DEVIATION-8 (الأخطر)**: قاعدة بيانات الويب تحتاج ملفين
   (`sqlite3.wasm`، `drift_worker.js`) لا يمكن تنزيلهما من هذه البيئة.
   بدونهما: الموقع سيُبنى وينشر بنجاح، لكن يفشل بصمت عند فتحه في
   المتصفح تحديدًا عند محاولة فتح القاعدة. **الخطوة الأولى الإلزامية على
   جهازك**: اتبع https://drift.simonbinder.eu/platforms/web/ .
2. **DEVIATION-9**: لم أكتب سكربت تحميل محرك Flutter يدويًا في
   `index.html` لأن الصيغة تختلف باختلاف نسخة Flutter لديك — تركته
   لـ`flutter build web` ليُدرجه تلقائيًا (السلوك القياسي في النسخ
   الحديثة).
3. **DEVIATION-12**: أرقام إصدارات الحزم الأربع الجديدة (`provider`،
   `shared_preferences`، `intl`، `sqlite3`) تقديرية وليست مؤكدة من
   pub.dev مباشرة.
4. لا مجلد `android/`/`ios/` بعد (DEVIATION-11) — لبناء تطبيق أندرويد
   حقيقي لاحقًا، شغّل `flutter create .` مرة واحدة على جهازك (أمر قياسي
   آمن، لا يمس `lib/`).

**كل هذا موثّق بالتفصيل في `DEVIATIONS.md` (DEVIATION-8 إلى 12).**

---

## 3. ما لم يُربط بعد (بصدق، لا إخفاء)

الأولويات (`PriorityStates`) والإتقان (`MasteryStates`) والذاكرة
(`MemoryStates`) ستظهر **فارغة** في الواجهة حتى الآن — ليس عيبًا في
الواجهة، بل لأن محركات BKT/FSRS/Priority المبنية في المراحل السابقة لم
تُربط بعد بحدث «إنجاز المهمة» (هذا هو Track D — التكامل، المسجَّل مسبقًا
كخطوة غير منجزة بعد). كل ما فعلته هذه الدفعة: عند إنجاز مهمة، يُكتب حدث
حقيقي دائم (`TaskCompleted`) في جدول Events — لا شيء يضيع، وأي ربط لاحق
لـTrack D سيجد هذه الأحداث بانتظاره. **راجع DEVIATION-10.**

---

## 4. خطوات التحقق المطلوبة على جهازك (بالترتيب)

```
flutter --version && dart --version
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze
flutter test                      # الاختبارات الموجودة مسبقًا فقط؛ لا اختبارات UI جديدة في هذه الدفعة
# --- إعداد الويب (مرة واحدة) ---
# اتبع https://drift.simonbinder.eu/platforms/web/ لوضع sqlite3.wasm و drift_worker.js في web/
flutter build web --release
# افتح build/web عبر: (cd build/web && python3 -m http.server 8000) وتصفّح 127.0.0.1:8000
```
ثم ادفع (git push) لتشغيل `deploy.yml` تلقائيًا، وتحقق من رابط Pages
الظاهر في تبويب Actions على GitHub.

---

## 5. الخطوة التالية بعد التحقق

بعد ظهور الموقع فعليًا في المتصفح: أضف `<link rel="canonical">` في
`web/index.html` ورابط `web/sitemap.xml` (المكانان المعلّمان بـTODO)،
ثم سجّل الرابط في Google Search Console — الخطوات التفصيلية لذلك جاهزة
عند الطلب في الدفعة التالية.

لا شيء من المنتج المتفق عليه سابقًا حُذف أو بُسِّط في هذه الدفعة.
