name: Unzip and deploy project

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract archive and flatten
        run: |
          unzip -o *.zip -d extracted_tmp
          if [ -d "extracted_tmp/student_app_foundation" ]; then
            cp -r extracted_tmp/student_app_foundation/* ./
          else
            cp -r extracted_tmp/* ./
          fi
          rm -rf extracted_tmp

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Flatten and deploy project files to root" || exit 0
          git push
